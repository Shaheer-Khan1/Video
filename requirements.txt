# Memory-optimized dependencies for 2-4GB instances
# Removed: whisper, moviepy, pillow, torch, numpy (saves ~1.5GB+ RAM)

fastapi==0.116.1
uvicorn==0.35.0
pydantic==2.11.7
python-dotenv==1.1.1
requests==2.32.4
imageio-ffmpeg==0.6.0

# Optional: Remove elevenlabs if using a different TTS API
elevenlabs==0.2.27
